openapi: 3.0.3
info:
  title: AETHER API
  version: 0.1.0
  description: >
    Contract for the AETHER backend. The frontend consumes typed clients
    generated from this file — the two sides never drift silently.

servers:
  - url: http://localhost:8080

tags:
  - name: auth

paths:
  /api/auth/register:
    post:
      tags: [auth]
      summary: Create an account (closed after the first one unless open-registration is enabled)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RegisterRequest" }
      responses:
        "201":
          description: Account created; refresh token set as an httpOnly cookie.
          headers:
            Set-Cookie: { $ref: "#/components/headers/RefreshCookie" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TokenResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/ApiError" }
        "409": { $ref: "#/components/responses/ApiError" }

  /api/auth/login:
    post:
      tags: [auth]
      summary: Authenticate with email and password
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "200":
          description: Authenticated; refresh token set as an httpOnly cookie.
          headers:
            Set-Cookie: { $ref: "#/components/headers/RefreshCookie" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TokenResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/ApiError" }

  /api/auth/refresh:
    post:
      tags: [auth]
      summary: Rotate the refresh token and mint a new access token
      description: Reads the `aether_refresh` cookie; each token is single-use.
      responses:
        "200":
          description: New access token; rotated refresh token set as cookie.
          headers:
            Set-Cookie: { $ref: "#/components/headers/RefreshCookie" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TokenResponse" }
        "401": { $ref: "#/components/responses/ApiError" }

  /api/auth/logout:
    post:
      tags: [auth]
      summary: Revoke the refresh token and clear its cookie
      responses:
        "204":
          description: Logged out.

  /api/auth/me:
    get:
      tags: [auth]
      summary: The authenticated user
      security:
        - bearerAuth: []
      responses:
        "200":
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UserResponse" }
          description: Current user.
        "401": { $ref: "#/components/responses/ApiError" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  headers:
    RefreshCookie:
      description: >
        `aether_refresh` — httpOnly, Secure, SameSite=Strict, path=/api/auth.
        Never readable by frontend code.
      schema: { type: string }

  responses:
    ApiError:
      description: Typed error; `code` is stable and translated by the frontend.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiError" }
    ValidationError:
      description: Bean-validation failure with per-field messages.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ApiError" }

  schemas:
    RegisterRequest:
      type: object
      required: [email, password, displayName]
      properties:
        email: { type: string, format: email, maxLength: 320 }
        password: { type: string, minLength: 8, maxLength: 72 }
        displayName: { type: string, maxLength: 100 }

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string }
        password: { type: string }

    TokenResponse:
      type: object
      required: [accessToken, user]
      properties:
        accessToken:
          type: string
          description: JWT, valid for 15 minutes; sent as `Authorization Bearer`.
        user: { $ref: "#/components/schemas/UserResponse" }

    UserResponse:
      type: object
      required: [id, email, displayName]
      properties:
        id: { type: string, format: uuid }
        email: { type: string, format: email }
        displayName: { type: string }

    ApiError:
      type: object
      required: [code, message, fields]
      properties:
        code:
          type: string
          description: >
            Stable machine-readable code, e.g. invalid_credentials,
            registration_closed, email_already_used, invalid_refresh_token,
            validation_failed.
        message: { type: string }
        fields:
          type: object
          additionalProperties: { type: string }
          description: Per-field messages when code is validation_failed.
